# Стандартный алгоритм умножения матриц


## Условия умножения
Матрицу **A** размером `m × n` можно умножить на матрицу **B** размером `n × p`.  
Результатом будет матрица **C** размером `m × p`.

## Математическая формула
Для каждого элемента `C[i][j]` результирующей матрицы:


C[i][j] = Σ (A[i][k] × B[k][j]) для k = 0..n-1


## Алгоритм (псевдокод)
```bash
for i = 0 to m-1:  
    for j = 0 to p-1:  
        C[i][j] = 0  
        for k = 0 to n-1:  
            C[i][j] += A[i][k] × B[k][j]  
```

## Пример:

A = | 1  2  3 |  
    | 4  5  6 | 
                
B = | 7  8   | \
    | 9  10  | \
    | 11 12  |

C[0][0] = A[0][0]×B[0][0] + A[0][1]×B[1][0] + A[0][2]×B[2][0]
        = 1×7 + 2×9 + 3×11 = 7 + 18 + 33 = 58

C = | 58  64 |
    | 139 154 |


## Сложность алгоритма
- **Временная сложность**: O(m × n × p) = O(n³) для квадратных матриц
- **Пространственная сложность**: O(m × p) для хранения результата



# Алгоритм Штрассена для умножения матриц



## 💡 Основная идея

Алгоритм использует **стратегию "разделяй и властвуй"** и основан на разбиении матриц на подматрицы и использовании специальных формул, позволяющих уменьшить количество рекурсивных умножений.

## 🔄 Ключевое отличие от наивного алгоритма

| Алгоритм | Рекурсивные умножения | Сложность |
|----------|----------------------|-----------|
| Наивный | 8 | O(n³) |
| Штрассена | 7 | O(n²·⁸¹) |

## 🛠️ Алгоритм шаг за шагом

### Шаг 1: Разбиение матриц
Для матриц A и B размера n×n (где n — степень двойки):

A = | A₁₁ A₁₂ | B = | B₁₁ B₁₂ |
    | A₂₁ A₂₂ |     | B₂₁ B₂₂ |



### Шаг 2: Вычисление промежуточных матриц
Создаем 7 матриц M₁-M₇:

M₁ = (A₁₁ + A₂₂) × (B₁₁ + B₂₂)
M₂ = (A₂₁ + A₂₂) × B₁₁
M₃ = A₁₁ × (B₁₂ - B₂₂)
M₄ = A₂₂ × (B₂₁ - B₁₁)
M₅ = (A₁₁ + A₁₂) × B₂₂
M₆ = (A₂₁ - A₁₁) × (B₁₁ + B₁₂)
M₇ = (A₁₂ - A₂₂) × (B₂₁ + B₂₂)



### Шаг 3: Вычисление результирующих подматриц

C₁₁ = M₁ + M₄ - M₅ + M₇
C₁₂ = M₃ + M₅
C₂₁ = M₂ + M₄
C₂₂ = M₁ - M₂ + M₃ + M₆



## 📊 Пример вычисления для матриц 2×2

### Исходные матрицы:
A = | 1 3 | B = | 6 8 |
    | 7 5 |     | 4 2 |

### Вычисляем M₁-M₇:

M₁ = (1 + 5) × (6 + 2) = 6 × 8 = 48
M₂ = (7 + 5) × 6 = 12 × 6 = 72
M₃ = 1 × (8 - 2) = 1 × 6 = 6
M₄ = 5 × (4 - 6) = 5 × (-2) = -10
M₅ = (1 + 3) × 2 = 4 × 2 = 8
M₆ = (7 - 1) × (6 + 8) = 6 × 14 = 84
M₇ = (3 - 5) × (4 + 2) = (-2) × 6 = -12


### Вычисляем результирующие подматрицы:

C₁₁ = M₁ + M₄ - M₅ + M₇ = 48 + (-10) - 8 + (-12) = 18
C₁₂ = M₃ + M₅ = 6 + 8 = 14
C₂₁ = M₂ + M₄ = 72 + (-10) = 62
C₂₂ = M₁ - M₂ + M₃ + M₆ = 48 - 72 + 6 + 84 = 66

### Результат:
C = | 18 14 |
    | 62 66 |



## ⚡ Сложность алгоритма

- **Временная сложность**: O(n^log₂7) ≈ O(n²·⁸¹)
- **Пространственная сложность**: O(n²)
- **Рекуррентное соотношение**: T(n) = 7T(n/2) + O(n²)

## 📈 Сравнение алгоритмов

| Параметр | Наивный алгоритм | Алгоритм Штрассена |
|----------|------------------|--------------------|
| Умножения | 8 рекурсивных   | 7 рекурсивных      |
| Сложения | 4                | 18                 |
| Сложность | O(n³) | O(n²·⁸¹) |
| Константа | Малая | Большая |

## ✅ Преимущества

- Асимптотически быстрее для больших матриц
- Теоретически важный результат
- Открыл путь для разработки других быстрых алгоритмов

## ❌ Недостатки

- Большая константа в O-нотации
- Нестабильность численных вычислений
- Сложность реализации
- Эффективен только для очень больших матриц
- Требует матрицы размера степени двойки

## 🎯 Практическое применение

- Используется для очень больших матриц (тысячи элементов в стороне)
- Часто применяется в комбинации с другими методами
- Используется в специализированных математических библиотеках
- Больше теоретическое значение, чем практическое



## Доказательство сложности алгоритма Штрассена

### Рекуррентное соотношение

Пусть $T(n)$ - количество арифметических операций для умножения матриц размера $n \times n$.

- **Рекурсивные вызовы**: 7 умножений матриц размера $\frac{n}{2}$ → $7T(\frac{n}{2})$
- **Сложения/вычитания**: Константное количество операций сложения матриц размера $\frac{n}{2}$ → $O(n^2)$

Получаем рекуррентное соотношение:

$$T(n) = 7T\left(\frac{n}{2}\right) + O(n^2)$$

### Решение методом дерева рекурсии

Построим дерево рекурсии:

- **Уровень 0**: 1 задача размера $n$, стоимость: $cn^2$
- **Уровень 1**: 7 задач размера $\frac{n}{2}$, стоимость: $7c(\frac{n}{2})^2 = \frac{7}{4}cn^2$
- **Уровень 2**: 49 задач размера $\frac{n}{4}$, стоимость: $49c(\frac{n}{4})^2 = (\frac{7}{4})^2cn^2$
- ...
- **Уровень k**: $7^k$ задач размера $\frac{n}{2^k}$, стоимость: $(\frac{7}{4})^kcn^2$

Глубина дерева: $\log_2 n$

### Суммарная сложность

$$T(n) = cn^2 \sum_{k=0}^{\log_2 n} \left(\frac{7}{4}\right)^k$$

Это геометрическая прогрессия со знаменателем $r = \frac{7}{4} > 1$. Сумма оценивается по последнему члену:

$$T(n) = \Theta\left(n^2 \cdot \left(\frac{7}{4}\right)^{\log_2 n}\right)$$

### Упрощение выражения

Преобразуем показатель степени:

$$\left(\frac{7}{4}\right)^{\log_2 n} = n^{\log_2 \frac{7}{4}} = n^{\log_2 7 - \log_2 4} = n^{\log_2 7 - 2}$$

Подставляем обратно:

$$T(n) = \Theta(n^2 \cdot n^{\log_2 7 - 2}) = \Theta(n^{\log_2 7})$$

## Итоговый результат

Таким образом, мы доказали что:

$$T(n) = \Theta(n^{\log_2 7}) \approx \Theta(n^{2.807})$$